from flask import Flask, request, render_template
from crewai import Agent

app = Flask(__name__)

@app.route(, methods=[GET,POST])
def home()
    respuesta = 
    if request.method == POST
        idea = request.form[idea]
        respuesta = fProyecto generado sobre {idea}
    return f
    h1Agente IA Proyectosh1
    form method=post
    Idea del proyectobr
    input name=idea
    brbr
    buttonGenerarbutton
    form
    h3{respuesta}h3
    
app = Flask(__name__)

@app.route("/")
def home():
    return "Sistema de Agentes IA funcionando 🚀"
app.run()
